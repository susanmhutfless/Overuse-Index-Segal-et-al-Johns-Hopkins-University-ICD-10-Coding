/* Indicator 4 */

/*Description from Excel file
(New) Number 		4	
Indicator 			Routine monitoring of digoxin in patients with congestive heart failure
Indicator
			Motivator: digoxin monitoring should not be done routinely; it should only be done if there is concern about digoxin toxicity 

			Indicator: digoxin concentration measurement (blood test) 

			[this can be reported among all adult patients]

Timing		Procedure code is associated with the inclusionarey diagnosis cade (same claim) and exclusionary diagnosis code is NOT associated with the procedure code (same claim)	

System		CV, internal medicine	

Actor		Hospitalists, cardiologists, internists, FPs

/*Updated with information from ICD-10 conversions_12_06-19*/


/*** start of indicator specific variables ***/

/*inclusion criteria*/
%global includ_hcpcs;
%global includ_pr10;

%let includ_hcpcs =
					'80162'					;

%let includ_dx10_3 =
					'I50'					;

%let includ_drg = ;

/** Exclusion criteria **/
%let EXCLUD_dx10=			'T46.0X1' 'T46.0X2' 'T46.0X3' 'T46.0X4' 'T46.0X54'; 

/** label pop specific variables  instructions: ctrl-H 4 for this pop # **/
%global flag_popped																;
%let 	flag_popped             		= popped4 								;
%let 	flag_popped_label				= 'indicator 4 popped'					;	
%let	flag_popped_dt					= popped4_dt							;
%let 	flag_popped_dt_label			= 'indicator 4 date patient popped'	;
%let 	pop_age							= pop_4_age							;				
%let	pop_age_label					= 'age eligible for pop 4'				;
%let	pop_los							= pop_4_los							;
%let	pop_los_label					= 'length of stay when patient popped'	;
%let	pop_year						= pop_4_year							;
%let	pop_nch_clm_type_cd				= pop_4_nch_clm_type_cd				;
%let  	pop_CLM_IP_ADMSN_TYPE_CD		= pop_4_CLM_IP_ADMSN_TYPE_CD			;
%let	pop_clm_fac_type_cd				= pop_4_clm_fac_type_cd				;
%let	pop_clm_src_ip_admsn_cd			= pop_4_clm_src_ip_admsn_cd					;
%let	pop_ptnt_dschrg_stus_cd  		= pop_4_ptnt_dschrg_stus_cd			;
%let	pop_admtg_dgns_cd				= pop_4_admtg_dgns_cd					;
%let	pop_icd_dgns_cd1				= pop_4_icd_dgns_cd1					;
%let	pop_clm_drg_cd					= pop_4_clm_drg_cd						;
%let	pop_hcpcs_cd					= pop_4_hcpcs_cd						;
%let	pop_OP_PHYSN_SPCLTY_CD			= pop_4_OP_PHYSN_SPCLTY_CD				;

%let	pop_nch_clm_type_cd_label		= 'claim/facility type for pop 4' 		;
%let	pop_CLM_IP_ADMSN_TYPE_CD_label	= 'inpatient admission type code for pop 4'	;
%let  	pop_clm_fac_type_cd_label		= 'inpatient clm_fac_type_cd for pop 4';
%let	pop_clm_src_ip_admsn_cd_label	= 'clm_src_ip_admsn_cd for pop 4'		;
%let	pop_ptnt_dschrg_stus_cd_label	= 'discharge status code for pop 4'	;	


/*** end of indicator specific variables ***/
